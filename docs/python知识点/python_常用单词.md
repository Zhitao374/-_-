# Python 一级考试·常用英文单词（含 turtle）

## 数据类型
`int` 整数 | `float` 浮点数 | `str` 字符串 | `bool` 布尔 | `True` 真 | `False` 假

## 输入输出与基本函数
`print` 输出 | `input` 输入 | `int()` 转整数 | `float()` 转浮点 | `str()` 转字符串

## 逻辑与条件
`if` 如果 | `not` 非 | `and` 与 | `or` 或

## 运算符与符号
`==` 等于 | `!=` 不等于 | `>=` 大于等于 | `<=` 小于等于 | `//` 整除 | `%` 取余 | `**` 幂

## 其他标识符与后缀
`Name` 变量名（大写） | `And` 变量名 | `Else` 变量名 | `Not` 变量名 | `.py` 文件后缀

## 开发环境相关
`IDLE` 自带IDE | `PyCharm` 开发环境 | `Jupyter Notebook` 笔记本环境

## turtle 海龟绘图库专用
`turtle` 海龟 | `import` 导入 | `setup` 设置 | `speed` 速度 | `pendown` 落笔 | `penup` 抬笔  
`pensize` 粗细 | `width` 粗细 | `pencolor` 画笔色 | `fillcolor` 填充色 | `color` 双色 | `bgcolor` 背景色  
`forward` 前进 | `backward` 后退 | `goto` 去到 | `circle` 圆 | `dot` 实心点 | `write` 写字  
`left` 左转 | `right` 右转 | `hideturtle` 隐藏 | `showturtle` 显示 | `home` 回家 | `clear` 清除 | `reset` 重置  
`begin_fill` 开始填充 | `end_fill` 结束填充